{
  "installCommand": "npm install && if [ -f frontend/package.json ]; then npm --prefix frontend install; fi && if [ -f backend/package.json ]; then npm --prefix backend install; fi",
  "buildCommand": "if [ -f frontend/package.json ]; then npm --prefix frontend run build; else echo 'Backend-only deployment: no frontend build'; fi",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "functions": {
    "api/index.js": {
      "maxDuration": 30
    }
  }
}
